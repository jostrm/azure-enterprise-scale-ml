{
  "folder": "${FACTORY_FOLDER}",
  "contract_version": 1,
  "action": "add-project-placements",
  "factory_id": "${FACTORY_ID}",
  "project_id": "${PROJECT_ID}",
  "expected_revision": "${CATALOG_REVISION}",
  "placements": [{"environment": "stage", "scale_set_id": "${STAGE_SCALESET_001_ID}"}]
}
