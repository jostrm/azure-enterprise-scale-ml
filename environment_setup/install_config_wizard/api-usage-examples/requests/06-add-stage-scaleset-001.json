{
  "folder": "${FACTORY_FOLDER}",
  "contract_version": 1,
  "action": "create-scale-set",
  "factory_id": "${FACTORY_ID}",
  "expected_revision": "${CATALOG_REVISION}",
  "scale_sets": [
    {
      "environment": "stage",
      "suffix": "001",
      "subscription_id": "${STAGE_SUBSCRIPTION_ID}",
      "tenant_id": "${TENANT_ID}",
      "orchestrator": "gha",
      "network": {"vnet_cidr": "172.18.0.0/18", "max_projects": 3}
    }
  ]
}
