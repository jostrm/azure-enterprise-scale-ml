{
  "folder": "${FACTORY_FOLDER}",
  "contract_version": 1,
  "action": "create-scale-set",
  "factory_id": "${FACTORY_ID}",
  "expected_revision": "${CATALOG_REVISION}",
  "scale_sets": [
    {
      "environment": "dev",
      "suffix": "002",
      "subscription_id": "${DEV_SUBSCRIPTION_ID}",
      "tenant_id": "${TENANT_ID}",
      "orchestrator": "gha",
      "network": {"vnet_cidr": "172.17.0.0/18", "max_projects": 3}
    }
  ]
}
