[pytest]
testpaths = .
norecursedirs = integration .* build dist test-results __pycache__
python_files = test_*.py
markers =
    integration: live Azure deployment tests (require LIVE_AZURE=1 and az login)
