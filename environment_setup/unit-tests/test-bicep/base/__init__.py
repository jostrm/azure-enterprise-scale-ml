"""Base layer: pure utilities with no AI Factory domain knowledge.

Once-and-only-once helpers shared by every test: path resolution, env/yaml
parsing, and thin wrappers around external CLIs (az, nslookup, bicep).
"""
