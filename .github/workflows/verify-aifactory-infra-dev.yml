name: verify-aifactory-infra-dev
on: push
jobs:
  bicep-job:
    runs-on: ubuntu-latest
    steps:
      - run: node --version
      - run: npm --version
      - run: python --version
      #run: bicep build ./src/main.bicep