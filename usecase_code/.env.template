# Sample template for local configuration
SUBSCRIPTION_ID=<TODO>
TENANT_ID=<TODO>
RESOURCE_GROUP=<TODO>

# =============================================================================
# AUTHENTICATION MODE CONFIGURATION
# =============================================================================
# Choose one of the authentication modes by setting AUTH_MODE:
#
# Option 1: User-Assigned Managed Identity (UAMI) - For Azure-hosted apps
# AUTH_MODE=uami
# AZURE_CLIENT_ID=<your-managed-identity-client-id>
#
# Option 2: User credentials (CLI/PowerShell/Interactive) - For local development
# AUTH_MODE=user
#
# Option 3: Default (not set) - Standard DefaultAzureCredential chain
# AUTH_MODE=
#
# =============================================================================

# Uncomment one of the configurations below:

# === FOR UAMI (Azure-hosted apps using managed identity) ===
# AUTH_MODE=uami
# AZURE_CLIENT_ID=<your-managed-identity-client-id-here>

# === FOR USER ===
# === FOR DEFAULT -> Leave AUTH_MODE unset or empty. [uami, user] 
AUTH_MODE=user
UAMI_FALLBACK_TO_USER=true

FOUNDRY_ACCOUNT_NAME="<TODO>"
FOUNDRY_PROJECT_NAME="aif2-<TODO>"
FOUNDRY_ACCOUNT_ENDPOINT="https://${FOUNDRY_ACCOUNT_NAME}.services.ai.azure.com"
FOUNDRY_PROJECT_ENDPOINT="${FOUNDRY_ACCOUNT_ENDPOINT}/api/projects/${FOUNDRY_PROJECT_NAME}"

AI_SEARCH_NAME="aisearchprj0<TODO>001"
AI_SEARCH_INDEX_NAME="a01-robot-joakim-001"

CHAT_MODEL="gpt-4o-mini-abcd"
EMBEDDING_MODEL="text-embedding-3-large"
EMBEDDING_DIMENSIONS="3072"

DEFAULT_QUERY="How to reset Robot-Joakim?"
AGENT_NAME="A01-CodeFirstAgent1"
AGENT_INSTRUCTIONS="You are a helpful assistant. You MUST ALWAYS use the azure_ai_search tool to answer every question - never answer from memory or general knowledge. If asked about Robot-Joakim, always use the AI Search tool."
FIRST_PROMPT="How to reset Robot-Joakim?"

FABRIC_AGENT_NAME="fabric-codefirstagent-001"
FABRIC_TOOL_CONNECTION="fabric_dataagent_xyz"
FABRIC_WORKSPACE_ID="<TODO>"
FABRIC_ARTIFACT_ID="<TODO>"
FABRIC_INSTRUCTIONS="You are a helpful assistant. You MUST ALWAYS use the fabric-codefirstagent-001 tool to answer every question - never answer from memory or general knowledge. If asked about sales, always use the fabric-codefirstagent-001 tool."
# Generate Project connection ID, as below template: 
PROJECT_CONNECTION_ID="/subscriptions/${SUBSCRIPTION_ID}/resourceGroups/${RESOURCE_GROUP}/providers/Microsoft.CognitiveServices/accounts/${FOUNDRY_ACCOUNT_NAME}/projects/${FOUNDRY_PROJECT_NAME}/connections/${FABRIC_TOOL_CONNECTION}"
FABRIC_TOOL_QUESTION_ARRAY='[{"question": "What are the total sales for everything in Sweden 2025? Answer only with a number. Nothing else. ", "answer": "1000"}]'
